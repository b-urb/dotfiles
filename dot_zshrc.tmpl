#!/bin/zsh
# ═══════════════════════════════════════════════════════════════════════════
# ⚠  AUTOGENERATED FILE - DO NOT EDIT MANUALLY
# ═══════════════════════════════════════════════════════════════════════════
# This file is managed by chezmoi. To make changes, edit the template
# partials in ~/.local/share/chezmoi/.chezmoitemplates/zsh/ and then
# run `chezmoi apply` to regenerate.
# ═══════════════════════════════════════════════════════════════════════════

# ─── Core modules ───────────────────────────────────────────────────────────
{{- template "zsh/00-env.tmpl" . }}
{{- template "zsh/10-zinit.tmpl" . }}
{{- template "zsh/20-completion.tmpl" . }}
{{- template "zsh/30-history.tmpl" . }}
{{- template "zsh/40-plugins.tmpl" . }}
{{- template "zsh/50-prompt.tmpl" . }}
{{- template "zsh/60-exports.tmpl" . }}
{{- template "zsh/70-aliases.tmpl" . }}
{{- template "zsh/80-functions.tmpl" . }}
{{- template "zsh/90-completions.tmpl" . }}

# ─── OS-specific configuration ──────────────────────────────────────────────
{{ if .isMac -}}
# ═══════════════════════════════════════════════════════════════════════════
# macOS-specific Configuration
# ═══════════════════════════════════════════════════════════════════════════
{{- template "zsh/os-darwin.tmpl" . }}
{{ else if .isLinux -}}
# ═══════════════════════════════════════════════════════════════════════════
# Linux-specific Configuration
# ═══════════════════════════════════════════════════════════════════════════
{{- template "zsh/os-linux.tmpl" . }}
{{ if .isUbuntu -}}
# ─────────────────────────────────────────────────────────────────────────
# Ubuntu/Debian-specific Configuration
# ─────────────────────────────────────────────────────────────────────────
{{- template "zsh/distro-ubuntu.tmpl" . }}
{{ else if .isArch -}}
# ─────────────────────────────────────────────────────────────────────────
# Arch Linux-specific Configuration
# ─────────────────────────────────────────────────────────────────────────
{{- template "zsh/distro-arch.tmpl" . }}
{{ end -}}
{{ end -}}
