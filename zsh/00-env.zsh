# Early environment options (before plugins)

[ -f "$HOME/.dotfiles/options/dotfiles.options.sh" ] && source "$HOME/.dotfiles/options/dotfiles.options.sh"
[ -f "$HOME/.dotfiles/options/dotfiles.options.local.sh" ] && source "$HOME/.dotfiles/options/dotfiles.options.local.sh"
