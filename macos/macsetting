defaults write -g NSAutomaticWindowAnimationsEnabled -bool false                                                                                                                                                                                ⛵ urban-dev()
defaults write -g NSWindowShouldDragOnGesture -bool true
